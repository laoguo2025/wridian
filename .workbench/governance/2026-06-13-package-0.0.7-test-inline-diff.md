# 2026-06-13 Wridian 0.0.7 测试版 exe 打包

## 目标

- 为 inline diff、聊天胶囊和一次性撤销修复生成无需安装的测试版 exe。
- 按项目约定保存在 `release/`，不纳入仓库。

## 过程

- 当前版本：`package.json` 和 `src-tauri/tauri.conf.json` 均为 `0.0.7`。
- 普通 `cmd.exe` 引号方式会被 PowerShell 拆坏 `Program Files (x86)` 路径；改用单参数传给 `cmd.exe /c`。
- 使用 VS 2022 BuildTools 环境执行：
  - `npm run tauri -- build --no-bundle`
- 复制 `src-tauri/target/release/wridian.exe` 到 `release/Wridian-0.0.7-test-20260613-0452.exe`。

## 验证

- Tauri no-bundle release 构建通过。
- 前端生产构建在 Tauri 打包过程中通过。
- 生成 exe 版本信息：
  - FileVersion: `0.0.7`
  - ProductVersion: `0.0.7`
  - ProductName: `Wridian`
- 最终本地测试版 exe：
  - Path: `release/Wridian-0.0.7-test-20260613-0452.exe`
  - Size: `16282112`
  - SHA256: `00379B8D4AAD67D8832F1AD10731793AF7D631FD4E4174C42C714DB8C4C84668`

## 回退

- 删除 `release/Wridian-0.0.7-test-20260613-0452.exe` 即可移除本次本地产物。
