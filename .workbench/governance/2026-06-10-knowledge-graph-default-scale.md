# Knowledge Graph Default Scale

## 背景

用户指出知识图谱默认显示过大，需要接近截图中的较小默认视图，并要求字体随圆点一起放大或缩小。

## 变更

- 默认适配相机使用 0.64 的缩放比例，不再把图谱铺满整个画布。
- 默认缩放上限降到 7.8，保留滚轮手动放大能力。
- 节点标签字号改为根据节点屏幕半径计算，随节点缩放同步变化。

## 验证

- `npm run build`：通过。
- `git diff --check`：通过，仅有 Windows 换行提示。
