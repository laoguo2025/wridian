# 2026-06-11 打包 0.0.4

## 范围

- 将 Wridian 应用版本从 `0.0.3` 升到 `0.0.4`。
- 基于当前已合入的知识图谱、混合检索、对话沉淀和写入锁更新重新生成 Windows 正式安装包。

## 变更

- 更新 `package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json` 的应用版本号。
- 更新 README 当前版本、下载入口和 `0.0.4` 更新说明。
- 生成并保存正式安装包：`release/Wridian-0.0.4-x64-setup.exe`。
- 安装包 SHA256：`CE51D14C12A0EDCC48C4F96C84B1BB3C40087E31962ED25AE907769C798DC811`。

## 验证

- `npm run build` 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\cargo-msvc.ps1 test --manifest-path src-tauri\Cargo.toml` 通过，45 个测试全部通过。
- `cmd.exe /c "<vcvars64.bat> && cd /d D:\Coding\Wridian && npm run tauri -- build"` 通过，生成 NSIS 和 MSI 包。
- 正式安装包已复制到 `release/Wridian-0.0.4-x64-setup.exe` 并核验 SHA256。
