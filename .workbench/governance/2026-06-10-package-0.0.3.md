# 2026-06-10 打包 0.0.3

## 范围

- 将 Wridian 应用版本从 `0.0.2` 升到 `0.0.3`。
- 重新生成 Windows Tauri release 包。

## 变更

- 更新 `package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json` 的应用版本号。
- 生成并保存：
  - `release/Wridian-0.0.3-test.exe`
  - `release/Wridian-0.0.3-x64-setup.exe`
  - `src-tauri/target/release/bundle/msi/Wridian_0.0.3_x64_en-US.msi`

## 验证

- `npm run build` 通过。
- `powershell -ExecutionPolicy Bypass -File scripts\cargo-msvc.ps1 test --manifest-path src-tauri\Cargo.toml --lib` 通过，36 个测试全部通过。
- `cmd.exe /c "<vcvars64.bat> && cd /d D:\Coding\Wridian && npm run tauri -- build"` 通过，生成 NSIS 和 MSI 包。
