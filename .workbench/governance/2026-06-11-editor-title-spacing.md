# 2026-06-11 编辑页标题与顶部留白修正

## 背景

用户指出文件编辑页顶部留白过大，并且标题区同时显示文件名小标题和大标题，视觉重复。

## 变更

- 编辑页 `.paper` 顶部留白改为与底部一致。
- 编辑态标题区删除文件名小标题，只保留大标题。
- 顶部操作行改为右对齐，只承载“添加选区到输入框”和保存状态。

## 验证

- `npm run build`：通过。
- `git diff --check`：无空白错误，仅有 CRLF 提示。
