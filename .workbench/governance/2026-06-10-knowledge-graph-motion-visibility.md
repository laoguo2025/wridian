# 2026-06-10 Knowledge Graph Motion Visibility

## 背景

测试版截图反馈知识图谱连线太细、动态图谱缺少之前的动效。

## 本轮变更

- 加粗知识图谱连线，并提高浅色主题下的可见度。
- 恢复动态图谱感：增加整图轻微呼吸浮动、连线流动虚线和节点轻微脉冲。
- 动效不再使用旧版单节点漂移，避免重新破坏“圆点和文字不可重叠”的约束。

## 验证

- `npm run build` 通过。

## 回退依据

- 本轮只涉及 `src/App.tsx` 的图谱 SVG 分层和 `src/App.css` 的图谱样式动画。
