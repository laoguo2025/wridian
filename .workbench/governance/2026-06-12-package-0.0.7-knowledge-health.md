# 2026-06-12 Package 0.0.7 Knowledge Health

## Scope

- Rebuild Wridian 0.0.7 Windows installer after the native knowledge health workflow refactor, health result action fixes, and cache visibility fixes.
- Copy final NSIS installer to `release/Wridian-0.0.7-x64-setup.exe`.

## Verification

- First plain `npm run tauri -- build` failed because the shell did not have MSVC `cl.exe`.
- Re-ran Tauri build through `vcvars64.bat`; build passed and produced NSIS and MSI bundles.
- Cleared stale `src-tauri/target/release/resources` before the final build to prevent old `zhishiku-skill` resources from leaking into the installer.
- Rebuilt again after `Hide knowledge cache files and persist health result`; final `release/Wridian-0.0.7-x64-setup.exe` SHA256: `5D601A6BAC430C0F52560932CF871D11489C2C195344300868234266008C26C8`.
- `src-tauri/target/release/wridian.exe` reports `FileVersion` and `ProductVersion` as `0.0.7`.
- MSI administrative extraction confirmed `resources/knowledge-health`, `resources/skills/work-decompose`, `resources/skills/knowledge-card`, and `resources/skills/author-distill` are packaged.
- MSI extraction search found no `zhishiku-skill`, `知识库运维`, or `knowledgeOps`.

## Artifacts

- `release/Wridian-0.0.7-x64-setup.exe`
- `src-tauri/target/release/bundle/nsis/Wridian_0.0.7_x64-setup.exe`
- `src-tauri/target/release/bundle/msi/Wridian_0.0.7_x64_en-US.msi`

## Rollback

- Restore the previous `release/Wridian-0.0.7-x64-setup.exe` from commit history or rebuild from the earlier 0.0.7 package commit.
