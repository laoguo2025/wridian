# 2026-06-14 Package 0.0.9 Claudian File Tools

## 背景

完成 Claudian 风格文件工具 fallback 后，按用户要求重新打包 0.0.9 安装包。

## 执行

- 版本保持 `0.0.9`：`package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`。
- 普通 PowerShell 直接运行 `npm run tauri -- build` 失败，原因是 release 编译依赖 `cl.exe`，当前 shell 未进入 MSVC Build Tools 环境。
- 使用 Visual Studio Build Tools `vcvars64.bat` 初始化环境后重新运行 `npm run tauri -- build` 成功。
- 生成并覆盖 `release/Wridian-0.0.9-x64-setup.exe`。

## 产物

- `release/Wridian-0.0.9-x64-setup.exe`
- 大小：4,571,864 bytes
- 生成时间：2026-06-14 00:30:03

## 验证

- `npm run build`：通过。
- `powershell -ExecutionPolicy Bypass -File scripts\cargo-msvc.ps1 test --manifest-path src-tauri\Cargo.toml cocreation --lib`：通过，42 个共创测试全绿。
- `npm run tauri -- build` in MSVC environment：通过，生成 MSI 和 NSIS 包。

## 回退

如安装包异常，可回退到上一版 `release/Wridian-0.0.9-x64-setup.exe` 对应提交 `dad5095`，或重跑同一 MSVC 环境打包命令生成新的 0.0.9 包。
