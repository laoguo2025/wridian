# Left Rail Current Directory

## 背景

用户指出作品库和知识库绑定本地文件夹后，左下角底部空位应显示当前目录。

## 变更

- 复用现有 `WorkspaceInfo` 中的作品库和知识库根目录字段。
- 左侧文件区底部新增只读文本：`当前目录：目录名`。
- 完整路径放在 `title`，避免窄侧栏展示长路径导致挤压设置按钮。
- 设置按钮保留在底部右侧。

## 非变化

- 不改变作品库/知识库选择文件夹逻辑。
- 不自动打开目录，不新增配置项。
- 未绑定目录时不显示该提示。

## 验证

- `npm run build`：通过。
